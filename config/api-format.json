{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "API Endpoint Configuration",
  "description": "Simple JSON config that points to code-based API routes",
  "type": "object",
  "required": ["resource", "routeFile"],
  "properties": {
    "resource": {
      "type": "string",
      "description": "Resource name (e.g., 'tasks', 'projects', 'users')"
    },
    "routeFile": {
      "type": "string",
      "description": "Path to the route file relative to src/api/routes/ (e.g., 'tasks.ts')"
    },
    "basePath": {
      "type": "string",
      "description": "Optional: override the base path (defaults to /api/{resource})"
    }
  }
}
